name: First Workflow

on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcomeeeeee"

      - name: CheckOut Repository
        uses: actions/checkout@v7
